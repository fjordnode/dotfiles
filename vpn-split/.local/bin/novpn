#!/bin/bash
if [ $# -eq 0 ]; then
    echo "usage: novpn <command> [args...]" >&2
    echo "Launches a command outside the VPN tunnel." >&2
    exit 1
fi

unit="novpn-$BASHPID-$RANDOM"
exec systemd-run --user --unit="$unit" --slice=novpn.slice --scope --collect -q -- "$@"
