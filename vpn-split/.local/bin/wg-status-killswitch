#!/bin/bash
if [ -f "${XDG_STATE_HOME:-$HOME/.local/state}/wg-killswitch-active" ]; then
    echo '{"text":"ks","icon":"shield-lock","color":"primary"}'
else
    echo '{"text":"ks","icon":"shield-off","color":"none"}'
fi
