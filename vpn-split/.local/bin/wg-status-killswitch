#!/bin/bash
# Emit ks label, green if the kill-switch table is active, gray otherwise.
if [ -f "${XDG_STATE_HOME:-$HOME/.local/state}/wg-killswitch-active" ]; then
    echo '<font color="#a6e3a1">ks</font>'
else
    echo '<font color="#6c7086">ks</font>'
fi
